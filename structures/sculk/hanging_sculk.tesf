num depth = 2 + randomInt(4);

num i = 0;
for (i; i < depth; i = i + 1) {
    if(getBlock(0, -i - 1, 0) != "minecraft:air") {
        break;
    }

    block(0, -i, 0, "minecraft:sculk", false);
}

block(0, -i, 0, "minecraft:sculk", false);